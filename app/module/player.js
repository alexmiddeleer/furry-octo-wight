angular.module('Player', ['Logger']);
angular.module('Player').service('Player', function(Logger) {

   var ex = {}

   ex.makeMove = function() {
      //check if move was made
      //if move was made, return it
      //otherwise, wait
   }

   return ex;
});
